// Google Sheets — publish each tab as CSV (File > Share > Publish to web > CSV)
// Then paste the URLs here.
export const PRODUCTS_SHEET_CSV_URL = import.meta.env
	.VITE_PRODUCTS_SHEET_CSV_URL;
export const GALLERY_SHEET_CSV_URL = "";

export const WHATSAPP_NUMBER = import.meta.env.VITE_WHATSAPP_NUMBER;
export const INSTAGRAM_URL = "https://instagram.com/mariadelia_guardapolvos";
